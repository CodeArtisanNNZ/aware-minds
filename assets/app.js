const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const STORAGE='aware-minds-product-v1';
const STAGES=[
 {id:'plan',name:'Plan',title:'Shape the project',help:'Define who it helps, what it does and the smallest useful first version.'},
 {id:'design',name:'Design',title:'Design pages and flow',help:'Plan navigation, screens and actions before writing code.'},
 {id:'build',name:'Build',title:'Build and edit files',help:'Open files, make focused changes and save them to the connected folder.'},
 {id:'data',name:'Data',title:'Connect the data',help:'Choose a database only when information must persist between visits.'},
 {id:'test',name:'Test',title:'Test the project',help:'Check behaviour, accessibility, security and the production build.'},
 {id:'git',name:'GitHub',title:'Review and save a version',help:'Inspect changes, write a clear commit message and push safely.'},
 {id:'publish',name:'Live',title:'Publish the project',help:'Choose hosting compatible with the project stack and connect a domain.'}
];
const LESSONS=[
 {cat:'Foundations',title:'How software fits together',quick:'Frontend is what people use; backend runs rules; a database saves information; hosting keeps it online.',detail:'A browser requests a page from a host. The frontend displays it and calls backend endpoints. The backend validates requests and reads or writes the database. Git records the source history.',fact:'Most small websites do not need every layer. A portfolio can be only HTML, CSS and JavaScript.'},
 {cat:'Foundations',title:'HTML, CSS and JavaScript',quick:'HTML gives structure, CSS controls presentation, and JavaScript adds behaviour.',detail:'HTML was proposed by Tim Berners-Lee around 1991. CSS was proposed by Håkon Wium Lie in 1994. Brendan Eich created JavaScript at Netscape in 1995.',fact:'Use semantic HTML first. It improves accessibility, search visibility and maintainability.'},
 {cat:'Languages',title:'JavaScript / TypeScript',quick:'Best for interactive websites and full-stack web applications. TypeScript adds static checking.',detail:'JavaScript runs natively in browsers and on servers through runtimes such as Node.js. TypeScript, released by Microsoft in 2012, catches many mistakes before execution.',fact:'Choose TypeScript for a growing team; plain JavaScript is fine for a small dependency-free site.'},
 {cat:'Languages',title:'Python',quick:'Best for automation, data, machine learning and readable backend services.',detail:'Guido van Rossum began Python in 1989 and released it publicly in 1991. Its readable syntax makes it approachable, but browser frontends normally still use JavaScript.',fact:'FastAPI and Django are strong web options. Python is not automatically faster or smarter than other languages.'},
 {cat:'Languages',title:'PHP',quick:'A practical server language for content sites, forms, dashboards and inexpensive hosting.',detail:'Rasmus Lerdorf created PHP in 1995. It powers many server-rendered sites and WordPress. Modern PHP supports types, packages and professional frameworks such as Laravel.',fact:'PHP works well on shared hosting; use prepared statements and never concatenate user input into SQL.'},
 {cat:'Languages',title:'Java',quick:'Strong for large backend systems, Android foundations and long-lived enterprise software.',detail:'James Gosling led Java at Sun Microsystems; it was released in 1995. The JVM enables consistent execution across operating systems.',fact:'Java has more ceremony than Python or JavaScript, but its tooling and type system suit large teams.'},
 {cat:'Database',title:'SQL and relational databases',quick:'Tables store structured rows. Keys connect related tables. SQL asks for or changes data.',detail:'PostgreSQL is feature-rich and open source; MySQL is common on web hosting; SQLite stores a whole database in one file and is ideal for local tools.',fact:'Use PostgreSQL for most new multi-user applications. Use SQLite for local or small single-server tools.'},
 {cat:'Database',title:'NoSQL databases',quick:'Document databases store flexible records, but flexibility does not remove the need for data design.',detail:'MongoDB stores JSON-like documents. Firebase provides managed realtime data. They are useful for changing schemas and realtime clients, but joins and reporting may be easier in SQL.',fact:'Do not choose NoSQL only because it sounds simpler. Relational data usually belongs in SQL.'},
 {cat:'Database',title:'Supabase and Firebase',quick:'Managed platforms combine databases, authentication and APIs. They reduce setup, not responsibility.',detail:'Supabase is built around PostgreSQL. Firebase is Google’s managed application platform. Both have free tiers with limits that can change.',fact:'Keep credentials in environment variables and enable row-level access policies before using real user data.'},
 {cat:'Git',title:'Repository, commit and branch',quick:'A repository is the project history. A commit is a named checkpoint. A branch is an independent line of work.',detail:'Git was created by Linus Torvalds in 2005. GitHub hosts Git repositories and collaboration tools; Git and GitHub are not the same thing.',fact:'A good commit is small, working and described with an action: “Fix mobile navigation”.'},
 {cat:'Git',title:'Fetch, pull and push',quick:'Fetch checks remote history. Pull brings remote changes into your branch. Push uploads your commits.',detail:'Fetch does not change working files. Pull normally fetches then integrates changes. Push can be rejected when the remote has changes you do not have.',fact:'Before pushing, review changed files, fetch the remote and resolve conflicts. Never use force push casually.'},
 {cat:'Interface',title:'Navbar, sidebar and routing',quick:'Navigation helps users move. Routing maps a URL to a screen. A navbar is not automatically the right pattern.',detail:'Use predictable labels, a visible active state and responsive navigation. Sidebars work for tool-heavy products; top navigation works for small public sites.',fact:'If users cannot predict what a button opens, visual decoration will not fix the navigation.'},
 {cat:'Interface',title:'Fuzzy search',quick:'Fuzzy search finds approximate matches even when spelling is imperfect.',detail:'It scores similarity rather than requiring exact text. Useful techniques include edit distance, token matching and weighted field ranking.',fact:'Always show why a result matched and allow exact filters for high-stakes data.'},
 {cat:'Quality',title:'Professional project checklist',quick:'Responsive layout, accessibility, validation, useful errors, security, testing, documentation and backups.',detail:'Test keyboard navigation, small screens, slow connections and failed requests. Validate on the server. Never commit secrets. Document setup and recovery.',fact:'A polished interface with unsafe data handling is not a professional product.'},
 {cat:'Hosting',title:'Static and server hosting',quick:'Static hosts serve built files. Server hosts run backend code. Choose by runtime, not popularity.',detail:'GitHub Pages and Cloudflare Pages suit static sites. Vercel suits frontend frameworks and serverless functions. Render and Railway can run servers. Availability and free-tier terms change.',fact:'A PHP/MySQL project cannot be deployed unchanged to a static-only host.'},
 {cat:'Hosting',title:'Domains, DNS and HTTPS',quick:'A domain is the name; DNS points it to hosting; HTTPS encrypts traffic.',detail:'Buy or receive a domain, add the host’s DNS records, wait for propagation, then verify HTTPS. Keep renewal ownership and recovery email under your control.',fact:'Never transfer a domain to an unknown person just to connect hosting.'},
 {cat:'Student',title:'GitHub Student Developer Pack',quick:'Verified students may receive GitHub and partner benefits. Offers and eligibility can change.',detail:'Apply through GitHub Education with current academic proof. Read every partner’s duration, renewal price and billing requirements before activating it.',fact:'Treat free domains and hosting as temporary benefits; plan what happens after graduation.'},
 {cat:'Tools',title:'VS Code essentials',quick:'Open the project folder, use Explorer to navigate files, Source Control to review Git, and Terminal to run project commands.',detail:'Use Ctrl+P for files, Ctrl+Shift+P for commands, Ctrl+` for terminal, Ctrl+Shift+F for project search and Ctrl+S to save.',fact:'Aware Minds guides the workflow; VS Code remains better for advanced debugging, extensions and refactoring.'}
];
const TOOL_LABELS={plan:'Roadmap',design:'Pages',build:'Files',data:'Database',test:'Checks',git:'GitHub',publish:'Publish'};
let state=load(), active=null, currentTool='plan', currentFile='';
function load(){try{return {...{theme:'dark',level:'Beginner',reduced:false,projects:[]},...JSON.parse(localStorage.getItem(STORAGE)||'{}')}}catch{return {theme:'dark',level:'Beginner',reduced:false,projects:[]}}}
function save(){localStorage.setItem(STORAGE,JSON.stringify(state))}
function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function toast(t){$('#toast').textContent=t;$('#toast').classList.remove('hidden');clearTimeout(toast.t);toast.t=setTimeout(()=>$('#toast').classList.add('hidden'),2600)}
function modal(html){$('#modalBody').innerHTML=html;$('#modal').classList.remove('hidden')}
function closeModal(){$('#modal').classList.add('hidden')}
function view(name){$$('.page').forEach(x=>x.classList.toggle('active',x.dataset.page===name));$$('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.view===name));$('#title').textContent={projects:'Projects',guide:'Build guide',learn:'Learn',settings:'Settings'}[name];$('#workbench').classList.add('hidden');$('#main').classList.remove('hidden');$('.sidebar').classList.remove('open')}
function prefs(){document.body.classList.toggle('light',state.theme==='light');document.body.classList.toggle('reduced',state.reduced);$('.level').textContent=state.level+'⌄';$('#levelSelect').value=state.level;$('#lightToggle').checked=state.theme==='light';$('#motionToggle').checked=state.reduced}
function renderProjects(){const has=state.projects.length>0;$('#empty').classList.toggle('hidden',has);$('#projectList').classList.toggle('hidden',!has);$('#projectList').innerHTML=state.projects.map(p=>`<article class="projectCard" data-project="${p.id}" style="--accent:${p.accent}"><div class="cover"><span>${esc(p.name.slice(0,2).toUpperCase())}</span></div><div class="projectMeta"><h3>${esc(p.name)}</h3><p>${esc(p.description)}</p><div class="progress"><i style="width:${p.progress||0}%"></i></div><small>${p.progress||0}% • ${esc(STAGES[p.stage||0].name)}</small></div></article>`).join('');$$('[data-project]').forEach(x=>x.onclick=()=>openProject(x.dataset.project))}
function wizard(){modal(`<small>NEW PROJECT · 1 MINUTE</small><h2>What are you building?</h2><p>Describe the goal first. Technology comes after the requirements.</p><form class="form" id="newForm"><label>Project name<input name="name" required maxlength="60" placeholder="Example: Local bakery website"></label><label>What should it help people do?<textarea name="description" required maxlength="240" placeholder="Example: View products and place an order"></textarea></label><label>Closest project type<select name="type"><option>Website</option><option>Portfolio</option><option>Web application</option><option>Mobile application</option><option>API / backend</option><option>Data project</option><option>Desktop tool</option></select></label><label>Accent colour<input name="accent" type="color" value="#6f8fae"></label><div class="formActions"><button type="button" data-action="closeModal">Cancel</button><button class="primary">Create workspace →</button></div></form>`);$('#newForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),p={id:crypto.randomUUID(),name:f.get('name').trim(),description:f.get('description').trim(),type:f.get('type'),accent:f.get('accent'),stage:0,progress:5,path:'',tasks:{},history:[],created:Date.now()};state.projects.unshift(p);save();closeModal();renderProjects();openProject(p.id)};wire($('#modalBody'))}
function openProject(id){active=state.projects.find(x=>x.id===id);if(!active)return;currentTool=STAGES[active.stage||0].id;$('#main').classList.add('hidden');$('#workbench').classList.remove('hidden');document.documentElement.style.setProperty('--accent',active.accent);$('#projectName').textContent=active.name;$('#projectType').textContent=active.type.toUpperCase();$('#projectMark').textContent=active.name.slice(0,2).toUpperCase();renderWorkbench()}
function renderWorkbench(){if(!active)return;$('#projectPath').textContent=active.path||'No folder connected';$('#tools').innerHTML=STAGES.map(s=>`<button data-tool="${s.id}" class="${s.id===currentTool?'active':''}">${TOOL_LABELS[s.id]}</button>`).join('');$$('[data-tool]').forEach(x=>x.onclick=()=>{currentTool=x.dataset.tool;renderWorkbench()});const i=STAGES.findIndex(s=>s.id===currentTool),s=STAGES[i];$('#stepLabel').textContent=`STEP ${i+1} OF ${STAGES.length}`;$('#stageTitle').textContent=s.title;$('#nextTitle').textContent=s.name+' — next action';$('#nextHelp').textContent=s.help;$('#timeline').innerHTML=STAGES.map((x,n)=>`<div class="timeStep ${n<(active.stage||0)?'done':n===(active.stage||0)?'current':''}" data-tool="${x.id}">${n+1}. ${x.name}<i></i></div>`).join('');$$('.timeStep').forEach(x=>x.onclick=()=>{currentTool=x.dataset.tool;renderWorkbench()});renderStage()}
function tasksFor(key,items){active.tasks[key] ||= [];return `<div class="taskList">${items.map((t,i)=>`<div class="task ${active.tasks[key].includes(i)?'done':''}"><button data-check="${key}:${i}">✓</button><span>${t}</span></div>`).join('')}</div>`}
function renderStage(){
 const content=$('#stageBody');
 if(currentTool==='plan')content.innerHTML=`<div class="panel"><small>PROJECT BRIEF</small><h3>${esc(active.name)}</h3><p>${esc(active.description)}</p>${tasksFor('plan',['Identify the primary user','Write one measurable project goal','Choose the smallest useful version','List features that are explicitly out of scope'])}</div>`;
 if(currentTool==='design')content.innerHTML=`<div class="panel"><small>USER FLOW</small><h3>Plan screens before styling them.</h3><p>A strong first flow is usually: entry → understand → act → confirmation.</p>${tasksFor('design',['List the required pages','Choose one primary action per page','Plan mobile navigation','Define loading, empty and error states'])}</div>`;
 if(currentTool==='build')renderFiles(content);
 if(currentTool==='data')content.innerHTML=`<div class="panel"><small>DATA DECISION</small><h3>Does this project need a database?</h3><p>Use one only for information that must survive page refreshes or be shared between users.</p><div class="serviceGrid"><div class="service"><b>SQLite</b><small>Local tools, prototypes, single server</small><button data-lesson="SQL and relational databases">Learn</button></div><div class="service"><b>PostgreSQL / Supabase</b><small>Multi-user web applications</small><button data-lesson="Supabase and Firebase">Learn</button></div><div class="service"><b>Firebase</b><small>Realtime mobile/web applications</small><button data-lesson="NoSQL databases">Learn</button></div></div>${tasksFor('data',['List the information to store','Draw tables or document shapes','Define ownership and access rules','Plan migrations and backups'])}</div>`;
 if(currentTool==='test')content.innerHTML=`<div class="panel"><small>QUALITY GATE</small><h3>Test the experience, not only the happy path.</h3>${tasksFor('test',['Run the project’s automated tests','Test keyboard-only navigation','Check phone, tablet and desktop layouts','Try empty, loading and error states','Confirm secrets are not committed','Run the production build'])}</div>`;
 if(currentTool==='git')renderGit(content);
 if(currentTool==='publish')content.innerHTML=`<div class="panel"><small>DEPLOYMENT</small><h3>Choose hosting that runs your stack.</h3><div class="serviceGrid"><div class="service"><b>Static / frontend</b><small>GitHub Pages, Cloudflare Pages, Vercel</small><button data-lesson="Static and server hosting">Compare</button></div><div class="service"><b>Node / Python server</b><small>Render, Railway, VPS providers</small><button data-lesson="Static and server hosting">Compare</button></div><div class="service"><b>PHP + MySQL</b><small>PHP-compatible shared or managed hosting</small><button data-lesson="Static and server hosting">Compare</button></div></div>${tasksFor('publish',['Create a production build','Configure environment variables on the host','Connect the repository and deploy','Connect DNS and verify HTTPS','Test the live site and recovery plan'])}</div>`;
 wire(content);$$('[data-check]').forEach(x=>x.onclick=()=>toggleTask(x.dataset.check));$$('[data-lesson]').forEach(x=>x.onclick=()=>openLesson(x.dataset.lesson))
}
async function renderFiles(content){
 content.innerHTML=`<div class="editorShell"><div class="fileTree" id="fileTree"><button data-action="folder">＋ Connect folder</button><p class="muted">Loading files…</p></div><div class="editor"><div class="editorTop" id="fileTitle">Choose a text file</div><textarea id="editorText" spellcheck="false" disabled></textarea><div class="editorActions"><button data-action="discard">Discard</button><button class="primary" data-action="saveFile">Save file</button></div></div></div>`;wire(content);
 if(!active.path){$('#fileTree').innerHTML='<button data-action="folder">＋ Connect a local project folder</button><p class="muted">Aware Minds will only access the folder you select.</p>';wire($('#fileTree'));return}
 try{const r=await api('/api/tree?id='+encodeURIComponent(active.id));$('#fileTree').innerHTML=r.files.map(f=>`<button data-file="${esc(f)}">${esc(f)}</button>`).join('')||'<p>No supported text files found.</p>';$$('[data-file]').forEach(x=>x.onclick=()=>openFile(x.dataset.file))}
 catch(e){$('#fileTree').innerHTML='<p>'+esc(e.message)+'</p><button data-action="folder">Reconnect folder</button>';wire($('#fileTree'))}
}
async function openFile(path){try{const r=await api('/api/file?id='+encodeURIComponent(active.id)+'&path='+encodeURIComponent(path));currentFile=path;$('#fileTitle').textContent=path;$('#editorText').value=r.content;$('#editorText').disabled=false;$$('[data-file]').forEach(x=>x.classList.toggle('active',x.dataset.file===path))}catch(e){toast(e.message)}}
async function saveFile(){if(!currentFile)return toast('Choose a file first');const before=await api('/api/file?id='+encodeURIComponent(active.id)+'&path='+encodeURIComponent(currentFile));const content=$('#editorText').value;if(before.content===content)return toast('No changes to save');modal(`<small>REVIEW EDIT</small><h2>Save ${esc(currentFile)}?</h2><p>This writes to your connected project folder. You can undo the most recent edit.</p><div class="changeList"><div class="task"><span>− ${before.content.length} characters</span></div><div class="task"><span>+ ${content.length} characters</span></div></div><div class="formActions"><button data-action="closeModal">Cancel</button><button class="primary" id="confirmSave">Apply change</button></div>`);wire($('#modalBody'));$('#confirmSave').onclick=async()=>{try{await api('/api/file',{method:'POST',body:JSON.stringify({id:active.id,path:currentFile,content})});active.history.push({path:currentFile,content:before.content,time:Date.now()});active.progress=Math.max(active.progress,45);active.stage=Math.max(active.stage,2);save();closeModal();renderWorkbench();toast('File saved. Review it before pushing.')}catch(e){toast(e.message)}}}
async function renderGit(content){content.innerHTML='<div class="panel"><small>GIT STATUS</small><h3>Review every change before it leaves your computer.</h3><div id="gitStatus">Checking repository…</div></div>';if(!active.path){$('#gitStatus').innerHTML='<p>Connect a local Git repository first.</p><button data-action="folder">Choose folder</button>';wire($('#gitStatus'));return}try{const r=await api('/api/git/status?id='+encodeURIComponent(active.id));$('#gitStatus').innerHTML=`<p><b>Branch:</b> ${esc(r.branch)} · ${r.ahead} ahead · ${r.behind} behind</p><div class="changeList">${r.files.length?r.files.map(f=>`<div class="task"><code>${esc(f.status)}</code><span>${esc(f.path)}</span></div>`).join(''):'<div class="task">Working tree is clean.</div>'}</div><button class="primary" data-action="push">Review & push</button>`;wire($('#gitStatus'))}catch(e){$('#gitStatus').innerHTML='<p>'+esc(e.message)+'</p>'}}
async function connectFolder(){try{const r=await api('/api/pick',{method:'POST',body:JSON.stringify({id:active.id})});if(r.cancelled)return;active.path=r.path;active.stage=Math.max(active.stage,2);active.progress=Math.max(active.progress,35);save();renderWorkbench();toast('Folder connected')}catch(e){modal(`<h2>Folder connection needs the desktop launcher</h2><p>${esc(e.message)}</p><p>Open <b>Start Aware Minds.cmd</b>, then try again.</p>`)}}
async function cloneRepo(){modal(`<small>CONTINUE FROM GITHUB</small><h2>Clone a repository</h2><p>Git authentication stays in Git Credential Manager. Aware Minds does not store passwords.</p><form class="form" id="cloneForm"><label>Repository URL<input name="url" required placeholder="https://github.com/owner/repository.git"></label><div class="formActions"><button type="button" data-action="closeModal">Cancel</button><button class="primary">Choose destination and clone</button></div></form>`);wire($('#modalBody'));$('#cloneForm').onsubmit=async e=>{e.preventDefault();try{const r=await api('/api/clone',{method:'POST',body:JSON.stringify({url:new FormData(e.target).get('url')})});if(r.cancelled)return;closeModal();const p={id:crypto.randomUUID(),name:r.name,description:'Imported from GitHub',type:'Existing project',accent:'#7185a8',stage:2,progress:35,path:r.path,tasks:{},history:[],created:Date.now()};state.projects.unshift(p);save();renderProjects();openProject(p.id)}catch(err){toast(err.message)}}}
async function pushFlow(){if(!active?.path)return toast('Connect a Git repository first');try{const r=await api('/api/git/status?id='+encodeURIComponent(active.id));modal(`<small>REVIEW & PUSH</small><h2>${r.files.length} changed file${r.files.length===1?'':'s'}</h2><p>Branch: <b>${esc(r.branch)}</b>. Remote: ${r.behind?'remote is '+r.behind+' commit(s) ahead—pull and resolve first.':'ready for review'}.</p><div class="changeList">${r.files.map(f=>`<div class="task"><code>${esc(f.status)}</code><span>${esc(f.path)}</span></div>`).join('')||'<div class="task">Nothing to commit.</div>'}</div><form class="form" id="pushForm"><label>Commit message<input name="message" required minlength="3" maxlength="100" placeholder="Example: Fix mobile navigation"></label><div class="formActions"><button type="button" data-action="closeModal">Cancel</button><button class="primary" ${!r.files.length||r.behind?'disabled':''}>Commit and push</button></div></form>`);wire($('#modalBody'));$('#pushForm').onsubmit=async e=>{e.preventDefault();try{const out=await api('/api/git/push',{method:'POST',body:JSON.stringify({id:active.id,message:new FormData(e.target).get('message')})});active.stage=Math.max(active.stage,5);active.progress=Math.max(active.progress,82);save();closeModal();renderWorkbench();toast(out.message)}catch(err){toast(err.message)}}}catch(e){toast(e.message)}}
async function api(url,opt={}){const r=await fetch(url,{headers:{'Content-Type':'application/json'},...opt});let data={};try{data=await r.json()}catch{}if(!r.ok)throw Error(data.error||'Desktop service is not running');return data}
function toggleTask(key){const [group,iRaw]=key.split(':'),i=Number(iRaw),arr=active.tasks[group]||[];active.tasks[group]=arr.includes(i)?arr.filter(x=>x!==i):[...arr,i];const done=Object.values(active.tasks).flat().length;active.progress=Math.min(96,Math.max(active.progress,5+done*4));const groupIndex=STAGES.findIndex(x=>x.id===group);if(active.tasks[group].length>=3)active.stage=Math.max(active.stage,Math.min(6,groupIndex+1));save();renderWorkbench();renderProjects()}
function renderGuide(){$('#guideGrid').innerHTML=STAGES.map((s,i)=>`<article class="guideCard"><strong>0${i+1}</strong><div><h3>${s.name}</h3><p>${s.help}</p></div><button data-guide="${s.id}">Open →</button></article>`).join('');$$('[data-guide]').forEach(x=>x.onclick=()=>{view('projects');state.projects.length?openProject(state.projects[0].id):wizard();currentTool=x.dataset.guide;active&&renderWorkbench()})}
function renderLessons(filter='All',q=''){const cats=['All',...new Set(LESSONS.map(x=>x.cat))];$('#lessonFilters').innerHTML=cats.map(c=>`<button class="${c===filter?'active':''}" data-filter="${c}">${c}</button>`).join('');const needle=q.toLowerCase();$('#lessonGrid').innerHTML=LESSONS.filter(x=>(filter==='All'||x.cat===filter)&&(!needle||JSON.stringify(x).toLowerCase().includes(needle))).map(x=>`<article class="lessonCard"><span class="tag">${x.cat}</span><h3>${x.title}</h3><p>${x.quick}</p><button data-lesson="${esc(x.title)}">Quick answer →</button></article>`).join('')||'<p>No matching lesson.</p>';$$('[data-filter]').forEach(x=>x.onclick=()=>renderLessons(x.dataset.filter,$('#learnSearch').value));$$('[data-lesson]').forEach(x=>x.onclick=()=>openLesson(x.dataset.lesson))}
function openLesson(title){const x=LESSONS.find(l=>l.title===title)||LESSONS[0];drawer(x.cat,x.title,`<article><h3>Quick answer</h3><p>${x.quick}</p><h3>Practical overview</h3><p>${x.detail}</p><p class="fact"><b>Useful fact</b><br>${x.fact}</p></article>`)}
function drawer(tag,title,html){$('#drawerTag').textContent=tag;$('#drawerTitle').textContent=title;$('#drawerBody').innerHTML=html;$('#drawer').classList.remove('hidden')}
function contextInfo(){if(active){const s=STAGES.find(x=>x.id===currentTool);drawer('CONTEXT GUIDE',s.name,`<article><h3>What is happening?</h3><p>${s.help}</p><h3>Why it matters</h3><p>You are at the ${s.name.toLowerCase()} stage of ${esc(active.name)}. Complete only the checks that apply to this project.</p><p class="fact">Your files are changed only after a visible confirmation.</p></article>`)}else{drawer('CONTEXT GUIDE',$('#title').textContent,'<article><h3>What is this page?</h3><p>Use Projects for real work, Build guide for the full path, and Learn when you want background knowledge.</p><p class="fact">You do not need to finish lessons before building.</p></article>')}}
function levelChooser(){modal(`<small>WORKSPACE DEPTH</small><h2>How much guidance do you want?</h2><div class="choices"><button data-level="Beginner"><b>Beginner</b><small>Plain language and one next action</small></button><button data-level="Practising"><b>Practising</b><small>Guidance plus technical names</small></button><button data-level="Professional"><b>Professional</b><small>Compact controls and direct tools</small></button></div>`);$$('[data-level]').forEach(x=>x.onclick=()=>{state.level=x.dataset.level;save();prefs();closeModal();toast('Workspace changed to '+state.level)})}
function commands(){const items=[['New project',()=>wizard()],['Open projects',()=>view('projects')],['Build guide',()=>view('guide')],['Learn Git basics',()=>openLesson('Repository, commit and branch')],['Connect project folder',()=>active?connectFolder():toast('Open a project first')],['Review and push',()=>pushFlow()],['Change experience level',()=>levelChooser()]];$('#paletteResults').innerHTML=items.map((x,i)=>`<button data-command="${i}">${x[0]}</button>`).join('');$('#palette').classList.remove('hidden');$('#paletteInput').value='';$('#paletteInput').focus();$$('[data-command]').forEach(x=>x.onclick=()=>{$('#palette').classList.add('hidden');items[Number(x.dataset.command)][1]()});$('#paletteInput').oninput=e=>{$$('[data-command]').forEach((b,i)=>b.classList.toggle('hidden',!items[i][0].toLowerCase().includes(e.target.value.toLowerCase())))}}
function safeCommand(){const t=$('#commandText').value.trim();if(!t)return toast('Describe an action first');const low=t.toLowerCase();let message='Aware Minds can preview: ';if(/delete|remove/.test(low))message+='a protected delete after file selection and confirmation.';else if(/replace|change/.test(low))message+='a search and replace. Open the target file so the exact change is visible.';else if(/add|upload/.test(low))message+='an upload after you select the destination folder.';else message+='this request as a checklist, but it will not execute unknown commands.';modal(`<small>SAFE COMMAND PREVIEW</small><h2>${esc(t)}</h2><p>${message}</p><p>No shell command or destructive edit has been executed.</p><div class="formActions"><button data-action="closeModal">Close</button></div>`);wire($('#modalBody'))}
function wire(root=document){$$('[data-action]',root).forEach(b=>b.onclick=()=>action(b.dataset.action))}
async function action(a){if(a==='skip')$('#splash').classList.add('hide');if(a==='home'||a==='back')view('projects');if(a==='new')wizard();if(a==='folder')active?connectFolder():modal('<h2>Open a project first</h2><p>Create a workspace, then connect its folder from the Files stage.</p>');if(a==='clone')cloneRepo();if(a==='repair')drawer('DIAGNOSTIC','Fix a project','<article><h3>Start with evidence</h3><p>Copy the complete error, note the command or action that caused it, and identify the first failing file—not the last symptom.</p><p class="fact">Change one cause at a time and keep a working Git checkpoint.</p></article>');if(a==='menu')$('.sidebar').classList.toggle('open');if(a==='theme'){state.theme=state.theme==='dark'?'light':'dark';save();prefs()}if(a==='level')levelChooser();if(a==='info')contextInfo();if(a==='commands')commands();if(a==='lost')drawer('RESCUE','You are not stuck',active?'<p>Open the current stage and follow the single action in the right panel. Use ⓘ for an explanation.</p>':'<p>Start with a project goal. You do not need to choose a language first.</p>');if(a==='closeDrawer')$('#drawer').classList.add('hidden');if(a==='closeModal')closeModal();if(a==='next'){const i=STAGES.findIndex(x=>x.id===currentTool);active.stage=Math.max(active.stage,Math.min(6,i+1));active.progress=Math.max(active.progress,Math.round(((i+1)/7)*100));save();currentTool=STAGES[Math.min(6,i+1)].id;renderWorkbench();renderProjects()}if(a==='push')pushFlow();if(a==='saveFile')saveFile();if(a==='discard'&&currentFile)openFile(currentFile);if(a==='undo'){const h=active?.history?.pop();if(!h)return toast('No edit to undo');try{await api('/api/file',{method:'POST',body:JSON.stringify({id:active.id,path:h.path,content:h.content})});save();currentFile=h.path;renderFiles($('#stageBody'));toast('Last edit restored')}catch(e){toast(e.message)}}if(a==='vscode'){try{await api('/api/open-vscode',{method:'POST',body:JSON.stringify({id:active.id})});toast('Opening VS Code')}catch(e){toast(e.message)}}if(a==='runCommand')safeCommand();if(a==='reset'){if(confirm('Delete all Aware Minds projects and preferences from this browser?')){localStorage.removeItem(STORAGE);location.reload()}}if(a==='preview')toast('Use your project’s development server or open its index.html to preview.')}
$$('#nav button').forEach(x=>x.onclick=()=>view(x.dataset.view));wire();$('#levelSelect').onchange=e=>{state.level=e.target.value;save();prefs()};$('#lightToggle').onchange=e=>{state.theme=e.target.checked?'light':'dark';save();prefs()};$('#motionToggle').onchange=e=>{state.reduced=e.target.checked;save();prefs()};$('#learnSearch').oninput=e=>renderLessons($('.filter .active')?.dataset.filter||'All',e.target.value);$('#modal').onclick=e=>{if(e.target.id==='modal')closeModal()};document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();commands()}if(e.key==='Escape'){closeModal();$('#drawer').classList.add('hidden');$('#palette').classList.add('hidden');$('#splash').classList.add('hide')}});setTimeout(()=>$('#splash').classList.add('hide'),3100);prefs();renderProjects();renderGuide();renderLessons();