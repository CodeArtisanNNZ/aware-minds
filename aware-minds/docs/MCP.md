# MCP

No MCP server is implemented. There is no MCP endpoint to test or configure. Scope-bearing server API keys do not become MCP OAuth credentials automatically. Implement a spec-compliant transport and authorization boundary before exposing resources/prompts/tools; the admin API must stay private.
