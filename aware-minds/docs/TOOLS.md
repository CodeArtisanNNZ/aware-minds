# Tools

No model-directed tool registry, agent loop, approval queue or code executor is enabled. Those paths cannot be QA-tested in this release candidate. Calculator/web/desktop actions do not exist; model text is never executed as shell or SQL. Future implementation must have trusted permission state and explicit approval for sensitive actions.
