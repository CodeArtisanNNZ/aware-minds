# Memory

Users explicitly create memories per application. No automatic extraction. Retrieval includes only the active user/app's last five memories, and a user setting can disable including them in chat. They are labelled untrusted contextual data. Tests cover cross-user/app isolation and disabling. Semantic search and global cross-app preferences are absent.
